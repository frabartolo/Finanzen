# Konto-Konfiguration
# WICHTIG: Zugangsdaten werden aus .env Datei geladen
accounts:
  - name: "Postbank Girokonto"
    type: "checking"
    bank: "Postbank"
    iban: "DE46370100500649213501"  # Ihre echte IBAN hier eintragen
    # FinTS-Konfiguration - Zugangsdaten aus .env
    blz: "37010050"  # Postbank BLZ
    login_name: "${POSTBANK_LOGIN}"  # Wird aus .env geladen
    pin: "${POSTBANK_PIN}"  # Wird aus .env geladen
    endpoint: "https://banking-be-1.postbank.de/fints30"  # Postbank FinTS-Endpoint
    
  - name: "Postbank Tagesgeld"
    type: "savings"
    bank: "Postbank"
    iban: "DE72550703240572916500"  # Falls vorhanden - Ihre echte IBAN
    # FinTS-Konfiguration (gleiche Zugangsdaten aus .env)
    blz: "55070324"
    login_name: "${POSTBANK_LOGIN}"
    pin: "${POSTBANK_PIN}"
    endpoint: "https://banking-be-1.postbank.de/fints30"
    
  - name: "Postbank Anlagekonto"
    type: "savings"
    bank: "Postbank"
    iban: "DE72550703240573427200"  # Falls vorhanden - Ihre echte IBAN
    # FinTS-Konfiguration (gleiche Zugangsdaten aus .env)
    blz: "55070324"
    login_name: "${POSTBANK_LOGIN}"
    pin: "${POSTBANK_PIN}"
    endpoint: "https://banking-be-1.postbank.de/fints30"
    
  - name: "Postbank Anlagekonto"
    type: "savings"
    bank: "Postbank"
    iban: "396573427200"  # Falls vorhanden - Ihre echte IBAN
    # FinTS-Konfiguration (gleiche Zugangsdaten aus .env)
    blz: "55070324"
    login_name: "${POSTBANK_LOGIN}"
    pin: "${POSTBANK_PIN}"
    endpoint: "https://banking-be-1.postbank.de/fints30"
    
  - name: "ING-DiBa Girokonto"
    type: "checking"
    bank: "ING-DiBa"
    iban: "DE12500105170123456789"  # Ihre echte ING IBAN hier eintragen
    # FinTS-Konfiguration für ING-DiBa
    blz: "50010517"  # ING-DiBa BLZ
    login_name: "${DIBA_LOGIN}"  # Wird aus .env geladen
    pin: "${DIBA_PIN}"  # Wird aus .env geladen
    endpoint: "https://fints.ing-diba.de/fints30"  # ING-DiBa FinTS-Endpoint
